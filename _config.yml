# ─────────────────────────────────────────
# TANGYSLICE COLLECTIVE — _config.yml
# ─────────────────────────────────────────

# Site identity
title:       Tangyslice Collective
description: "The Agency/SaaS Industrial Complex was built for a buyer journey AI just ended. We build what comes next — custom AI workflows, fractional CMO leadership, and tools you own."
url:         "https://tangyslice.com"
baseurl:     ""

# Default social share image (1200x630px)
# Place at assets/img/og-default.jpg
og_image: "/assets/img/og-default.jpg"

# Author defaults (used in Dispatch post meta)
author:
  name: Tangyslice Collective

# Build settings
markdown:  kramdown
permalink: /:title/

# Collections
collections:
  posts:
    output: true
    permalink: /dispatch/:title/

# Default front matter
defaults:
  # All pages
  - scope:
      path: ""
      type: pages
    values:
      layout: base
  # All Dispatch posts
  - scope:
      path: ""
      type: posts
    values:
      layout: dispatch-post

# Exclude from build
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - .gitignore
  - vendor/
